// Prueba de ejemplo para la API de productos.
// Más adelante puedes usar Jest, Mocha o cualquier otra librería.

describe('Productos', () => {
  test('debe existir la carpeta de pruebas', () => {
    expect(true).toBe(true);
  });
});
